# uDocket — Config Agents Guide

Configuration guidance is centralized in the root [AGENTS.md](../AGENTS.md). Follow that document for environment variables, secrets handling, and pipeline activation rules. Add any config-specific context to the root guide (mentioning `config/`) rather than this pointer file.
