# Promptsets

Prompt assets now live directly under `packages/ai/promptsets`. Each domain/locale folder contains Markdown+front matter templates hashed and validated via `packages.common.prompts`.

Refer to docs/overview/tdd/appendices/repository_trees.md for the binding layout contract.
