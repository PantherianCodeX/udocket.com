# uDocket Auto Image Scale Plugin

Local MkDocs plugin that inspects generated HTML and injects explicit
`width`/`height` attributes for images that opt in via a data attribute,
a CSS class, or an optional default scale. This keeps rendered images
consistent without manually editing every Markdown file.
