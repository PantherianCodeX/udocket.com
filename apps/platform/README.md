# uDocket Platform

This directory hosts the Django platform application shipped inside the main
stack. See the repository root `README.md` for setup instructions. The file
exists to keep packaging tools happy when building the project metadata.
